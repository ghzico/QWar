# Tasks: 战斗成功/失败界面

## 1. 结果界面 UI 与资源路径

- [x] 1.1 创建 `Res/BattleResult/` 目录，并在该目录或项目文档中说明：胜利图命名为 `victory.png`、失败图命名为 `defeat.png` 放入此处即可显示（本期可留空）
- [x] 1.2 新增战斗结果界面（单一面板或场景），支持「胜利/失败」两种模式：显示对应标题文本（「战斗胜利！」/「战斗失败...」）、结果图区域（从 `res://Res/BattleResult/victory.png` 或 `defeat.png` 加载，缺失时隐藏或占位）、以及对应按钮（胜利：回到主菜单；失败：重新挑战、回到主菜单）

## 2. 胜负判定与触发

- [x] 2.1 在 FightBoard 或战斗场景中实现胜负检测：无怪物→胜利；存在怪物且无存活棋将→失败；在怪物或棋将从棋盘移除后调用检测
- [x] 2.2 当判定为胜利时显示战斗胜利界面；当判定为失败时显示战斗失败界面；显示后屏蔽或暂停棋盘进一步操作（避免重复触发）

## 3. 按钮逻辑

- [x] 3.1 「回到主菜单」：调用 `GetTree().ChangeSceneToFile("res://Scenes/MainMenu.tscn")`
- [x] 3.2 「重新挑战」：调用 `GetTree().ChangeSceneToFile("res://Scenes/TestScene.tscn")`（或当前战斗场景路径）以重新进入本局

## 4. 集成与验证

- [x] 4.1 在怪物死亡移除、棋将死亡移除的代码路径中接入胜负检测调用，确保两种结局都能正确弹出对应界面
- [ ] 4.2 验证：怪物全灭后出现胜利界面并可回到主菜单；棋将全灭且仍有怪物时出现失败界面，可重新挑战或回到主菜单；结果图路径下无图时界面仍正常、不报错
