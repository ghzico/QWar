# Tasks: 技能/攻击距离显示与棋将包系统

## 1. 攻击/技能范围高亮与执行判定

- [x] 1.1 FightBoard 暴露或复用 GetCellsWithinRange(row, col, range)，供攻击/技能高亮使用（返回攻击距离内所有格子）
- [x] 1.2 ChessHero 选择「攻击」时，用攻击距离内所有格子（GetCellsWithinRange）调用 SetHighlightCells，不再仅用 GetEnemyCellsInAttackRange
- [x] 1.3 ChessHero 选择「技能」时，同样用技能攻击距离内所有格子做高亮
- [x] 1.4 棋盘点击处理：当处于攻击/技能选目标状态且玩家点击高亮格时，若该格内有敌将则执行攻击/技能；若格内无目标则不执行并保持或取消选择

## 2. 棋将包 UI 与数据加载

- [x] 2.1 新增棋将包场景或 UI 节点（如 DeckPanel），放置在战斗场面下方，布阵阶段可见
- [x] 2.2 棋将包从 GeneralConfigLoader.LoadHeroConfigs() 加载可选棋将，展示立绘（PortraitPath）与可区分标识（如 ID）
- [x] 2.3 战斗入口支持「布阵阶段」：进入后先显示棋将包与棋盘第 1/2 列，未确认出战前不启动回合与怪物 AI

## 3. 拖拽布阵逻辑

- [x] 3.1 实现从棋将包拖拽到地图的拖拽逻辑；仅当目标格子为第 1 列（col=1）或第 2 列（col=2）且该格为空时接受放置
- [x] 3.2 放置成功后在目标格生成棋将实例并调用 ApplyToHero 应用对应 HeroConfig；该棋将从包内移除或标记为已使用
- [x] 3.3 本场战斗中每个棋将（按配置 ID）只能被拖拽上场一次，已上场的不可再次从包内拖出

## 4. 确认出战与战斗开始

- [x] 4.1 添加「确认出战」按钮；点击后将当前已布阵棋将锁定为本局我方单位，关闭或隐藏棋将包/布阵 UI，并启动战斗（触发我方回合开始）
- [x] 4.2 确保布阵阶段未点击确认前，回合系统不运行、怪物不执行任何行动

## 5. 文档与验证

- [x] 5.1 若对 General.xlsx 使用方式有扩展，在 OPENSPEC.md 或本变更说明中补充棋将包数据来源约定
- [x] 5.2 验证：选择攻击/技能后所有范围格高亮、仅点击有敌将的格才执行；布阵阶段拖拽到第 1/2 列、确认出战后战斗开始、每棋将仅可上场一次
